#!/bash/sh

if ! pacman -Qs neovim > /dev/null ; then
	sudo pacman -Sy neovim --noconfirm
fi

if ! pacman -Qs ripgrep > /dev/null ; then
	sudo pacman -Sy ripgrep --noconfirm
fi

DIR=~/.config/nvim
[ -d "$DIR" ] && rm -rf "$DIR"

mkdir $DIR

cp -r nvim-configs/* $DIR

echo 'nvim installed !!'
